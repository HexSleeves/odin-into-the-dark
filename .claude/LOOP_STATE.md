# Loop State — Into the Depths

The spine of the autonomous loop. The agent forgets between runs; this file does not.
Each loop tick reads this first, acts, then writes back. The GitHub Project board is the
authoritative work tracker — this file is the loop's working ledger (what was tried, what
failed, what's mid-flight) so the next tick picks up where this one stopped.

> Board: `HexSleeves/odin-into-the-dark` · Project `PVT_kwHOAI2S3s4BZire`
> Gate: `just verify` must be green before anything is marked done.

---

## In progress

_(one entry max per worktree; the tick owns it until verified or abandoned)_

- _none_

## Open — triaged, ready to pick

_(written by the loop-triage skill, highest-value first)_

- _none yet — run the triage skill_

## Done this cycle

_(cleared at the start of each fresh triage run)_

- _none_

## Tried + failed — do NOT retry blindly

_(why it failed, so the loop doesn't loop on the same rock)_

- _none_

## Notes for next tick

- _none_
