# Implementation Plan — Audit 2026-06-13

Consolidated apply plan for 24 implementation specs derived from `docs/audit-2026-06-13.md`.
All specs have `depends_on: []`; apply order is driven by criticality, file-locality, and the
category sequence (criticals → cleanup → optimize → input → gameplay tuning → features → ci/docs/tests).

`just verify` + `just fmt` are the completion gate for every batch.

---

## ⚠️ CALLOUTS — read before applying

### `needs-decision` items
None of the 24 specs carry status `needs-decision`. **However**, several `ready` items embed
high-impact open questions that MUST be answered before/while applying:

- **M1-P5 (save-format v11, risk HIGH)** — Save-compat contract is undecided. Per project rule
  "delete old paths by default unless explicit compat contract", the default is to DROP legacy
  v2–v10 read support, which collapses this item from HIGH→LOW effort and removes the
  `Tile_V10`/`Save_Data_V10` freeze entirely. **Decide before applying.**
- **M1-P5 vs D4 collision** — BOTH bump the on-disk save to v11 and add a `Save_Data_V10` freeze +
  migration branch. They CANNOT both independently "become v11". They must land in the SAME batch,
  sequenced (M1-P5 first establishes v11 + the v10 freeze; D4 then appends `tutorial_flags` as a
  trailing field to the already-bumped v11 `Save_Data`). See Batch 6.
- **P4 (render cache)** — recommends landing only the dedup half now and DEFERRING the per-tile
  color cache until P8 profiling exists. Plan adopts this: dedup + dead-field deletion only.
- **R11 / D3** — pause-menu and level-up menu UX questions (process-quit option? mandatory reward?
  full heal?). Non-blocking; ship spec defaults, flag for design.

### `already-fixed` items (production code already in working tree; deliver test/commit only)
- **P1** (temp-allocator frame arena) — 2-line engine fix already uncommitted; **add regression test + commit**.
- **R10** (cardinal movement guard) — code already present; **add regression test only**.
- **D6** (depth-merchant currency) — `cost_id="iron_ore"` already staged; **add regression test only**.
- **docs-drift-readme-structure** — README + CLAUDE.md already corrected; **optional README corruption cleanup only**.
- **C1 / C2 / T1 / T2** are partly-landed in the working tree (atomic-write, clamp, ci.yml,
  roundtrip tests) — see per-item notes; these need the missing half (load `.bak` fallback, test
  repair, duplicate-flag removal, one wrong assertion fix) plus committing the in-tree work.

---

## Summary table

| id | title | category | status | risk | effort | behavior_change | feature_flag | depends_on |
|----|-------|----------|--------|------|--------|-----------------|--------------|------------|
| C1 | Atomic save writes: finish .bak recovery + tests | reliability | ready | low | low | yes | — | — |
| C2-R1-save-deser-clamp | Save deser count-clamp: repair migration test + clamp coverage | reliability | ready | low | low | no | — | — |
| E4 | Delete dead engine code (Energy_Actor, scene_update, camera_manager_abs) | cleanup | ready | low | trivial | no | — | — |
| G6 | Delete dead g_data registry + data_load_all wrapper | cleanup | ready | low | trivial | no | — | — |
| G4 | De-duplicate FOV shadowcast into gcore.compute_fov | cleanup | ready | low | low | no | — | — |
| G5 | De-duplicate floor-snapshot lifecycle into gcore | cleanup | ready | low | low | no | — | — |
| M1-P5 | Data-model diet: drop dead Tile fields; serialize Tile_State directly | cleanup | ready | high | high | no | — | — |
| P1 | temp_allocator → per-frame arena (commit + lock) | optimize | already-fixed | low | trivial | yes | — | — |
| P3 | Cache parsed scores.json; reload only on change | optimize | ready | low | low | no | — | — |
| P2 | Minimap: scratch index buffer for enemy/npc lookup | optimize | ready | low | low | no | — | — |
| P4 | Dedup render_map tile-color into get_tile_color (+optional cache) | optimize | ready | low | low | no | — | — |
| P6-P7 | Dijkstra BFS hoist (P6) + flow-field cache across rounds (P7) | optimize | ready | low | low | no | — | — |
| R10 | Force cardinal movement — add regression test | test | already-fixed | low | trivial | no | — | — |
| R11 | Route Escape → Pause / confirm menu | input | ready | low | med | yes | — | — |
| D6 | Depth-merchant currency fix — add regression test | gameplay | already-fixed | low | trivial | yes | — | — |
| D2 | Early-game tuning: starter leather vest + re-band enemies | gameplay | ready | low | low | yes | — | — |
| D8 | Combat feedback: floating damage numbers + poison_cloud ability | gameplay | ready | med | med | yes | — | — |
| D1 | Player light → enemy detection (flagged) | gameplay | ready | low | low | no | LIGHT_AFFECTS_DETECTION | — |
| D3 | Milestone kill-driven level-up menu | gameplay | ready | low | med | yes | LEVELUP_ENABLED | — |
| D7 | Numeric run score + victory multiplier | gameplay | ready | low | low | yes | — | — |
| D4 | First-encounter onboarding hints (bit_set on Game) | feature | ready | med | med | yes | — | — |
| T1 | Fix CI: drop duplicate -collection:libs flag | ci | ready | low | trivial | yes | — | — |
| docs-drift-readme-structure | README/CLAUDE.md docs drift | docs | already-fixed | low | trivial | no | — | — |
| T2 | Save-migration regression tests: fix one V4 assertion | test | ready | low | trivial | no | — | — |

**Counts by category:** reliability 2 · cleanup 5 · optimize 5 · test 2 · input 1 · gameplay 6 · feature 1 · ci 1 · docs 1 (total 24).
**Counts by risk:** low 21 · med 2 (D8, D4) · high 1 (M1-P5).
**Counts by status:** ready 19 · already-fixed 5.

---

## Batch plan

Ordering rules applied: criticals (C1, C2) first; cleanup/consolidation (E4, G6, G4, G5) before items
touching the same files; the HIGH-risk save-data diet (M1-P5) isolated and sequenced with the other
save-format mutator (D4); optimize; input; gameplay tuning; features; ci/docs/tests last.

### Batch 1 — `fix: harden save persistence (atomic + clamp)`
- **Items:** C1, C2-R1-save-deser-clamp
- **Files:** `src/io/save_restore.odin`, `test/engine/storage_manager_test.odin`,
  `test/io/save_atomic_write_test.odin`, `test/io/save_migration_test.odin`
- **Risk:** low
- **Apply note:** Critical reliability first. C1 adds the load-side `.bak` fallback + commits the
  already-in-tree atomic-write/CRC work; C2 repairs the failing V8 migration test (legacy 8-byte
  header) and adds clamp/round-trip coverage. Disjoint test files; both only edit `save_restore.odin`
  (C1) and `save_migration_test.odin` (C2) — no overlap with each other beyond the shared save layer
  they validate. Land before any save-format mutation (Batch 6).

### Batch 2 — `refactor: delete dead engine + content code`
- **Items:** E4, G6
- **Files:** `src/engine/turn_manager.odin`, `src/scene.odin`, `src/engine/camera_manager.odin`,
  `src/data.odin`, `src/content_manager.odin`, `src/core/data_defs.odin`,
  `test/content_manager_test.odin`
- **Risk:** low
- **Apply note:** Pure dead-code deletion, no behavior change. E4 removes Energy_Actor / scene_update /
  camera_manager_abs (audit corrections: input_tick doesn't exist, reset_repeats is LIVE — do NOT
  delete). G6 removes g_data + data_load_all. Note both touch `src/scene.odin`? No — only E4 touches
  scene.odin; G6 is data/content. Disjoint files. Run early to shrink surface before consolidation.

### Batch 3 — `refactor: de-duplicate FOV and floor-snapshot helpers into gcore`
- **Items:** G4, G5
- **Files:** `src/core/fov.odin`, `src/core/floor_snapshot.odin`, `src/io/save_common.odin`,
  `src/gameplay/fov.odin`, `src/gameplay/common.odin`, `src/gameplay/generation.odin`,
  `src/gameplay/game_lifecycle.odin`, `test/core/fov_core_test.odin`,
  `test/floor_snapshot_lifecycle_test.odin`
- **Risk:** low
- **Apply note:** Both consolidate duplicated logic into package gcore and both EDIT
  `src/io/save_common.odin` — must land together. G4's open question confirms disjoint line ranges
  (FOV block vs snapshot helpers) so no conflict. Apply G4 then G5. This MUST precede M1-P5, which
  re-plumbs the same `save_common.odin` aliases and `generation.odin` snapshot paths.

### Batch 4 — `perf: grid + cache hot-path optimizations`
- **Items:** P3, P2, P4, P6-P7
- **Files:** `src/render/scores.odin`, `src/render/clay_minimap.odin`, `src/render/render_map.odin`,
  `src/engine/distance_map.odin`, `src/ai/enemy_pathfinding.odin`, `src/ai/enemy_turns.odin`,
  `src/core/types.odin`, `src/core/game_utils.odin`, `test/score_manager_test.odin`,
  `test/render_minimap_test.odin`, `test/test_imports_test.odin`, `test/render/render_map_color_test.odin`,
  `test/engine/distance_map_test.odin`, `test/enemy_pathfinding_test.odin`
- **Risk:** low
- **Apply note:** Four independent optimizations grouped as one perf commit. P4 + P6-P7 both touch
  `src/core/types.odin` (Game struct) — P4 deletes dead render-cache fields, P6-P7 adds
  `dijkstra_dirty`; non-overlapping field edits, apply sequentially. P4 lands DEDUP-ONLY per its open
  question (defer color cache to post-P8). P6-P7 requires touching 5 direct `process_enemy_turns`
  test callers to set `dijkstra_dirty=true` — do not skip. P2/P3 are isolated.

### Batch 5 — `feat(input): route Escape to a pause/confirm menu`
- **Items:** R11, R10
- **Files:** `src/core/types.odin`, `src/scene.odin`, `src/input/playing_action.odin`,
  `src/input/state_updates.odin`, `src/input/state_shared.odin`, `src/render/clay_screen_ui.odin`,
  `src/render/clay_overlays.odin`, `test/input_pause_menu_test.odin`,
  `test/input_cardinal_movement_test.odin`
- **Risk:** low
- **Apply note:** R11 removes the in-tree double-Escape `quit_armed` global and replaces it with a
  real Pause scene (new Game_State/Game_Scene + mapping + overlay). R10 is already-fixed code — adds
  the cardinal-movement regression test only; bundled here because both are input-layer and R11 edits
  `playing_action.odin` adjacent to R10's guard. Adds new enum members to `types.odin` (Game_State
  .Pause) — keep this BEFORE the gameplay batch that also extends Game_State (D3 .Viewing_Level_Up,
  D4 has none) to avoid enum-reorder churn; append new variants at the end.

### Batch 6 — `refactor: shrink save data model (Tile diet + tile-state carrier)`
- **Items:** M1-P5
- **Files:** `src/core/types.odin`, `src/core/game_utils.odin`, `src/io/save_format.odin`,
  `src/io/save_write.odin`, `src/io/save_restore.odin`, `src/io/save_migrations.odin`,
  `src/io/save_convert.odin`, `src/io/save_common.odin`, `src/gameplay/generation.odin`,
  `src/gameplay/common.odin`, `test/tile_state_saveload_test.odin`, `test/io/save_migration_test.odin`
- **Risk:** **high**
- **Apply note:** ISOLATED batch — the only HIGH-risk item and the save-format v10→v11 bump. Must land
  AFTER Batch 1 (save hardening) and Batch 3 (save_common consolidation) and BEFORE D4 (which appends
  to v11). **DECISION GATE:** if no shipped/tagged save format must be honored, DROP legacy v2–v10
  migration entirely (collapses to low effort, deletes the Tile_V10 freeze). Recommended layout:
  append `tile_states` as the LAST field of Save_Data/Save_Floor. Self-contained commit; verify Game
  struct shrinks (~32 KB) via size assertion.

### Batch 7 — `feat(gameplay): early-game tuning, combat feedback, progression, scoring`
- **Items:** D6, D2, D7, D8, D1, D3
- **Files:** `src/gameplay/events.odin`, `src/gameplay/items.odin`, `src/gameplay/actions.odin`,
  `src/gameplay/restart.odin`, `src/gameplay/common.odin`, `src/core/content_ids.odin`,
  `src/core/types.odin`, `src/core/gameplay_tuning.odin`, `src/core/game_utils.odin`,
  `src/core/service_ids.odin`, `data/enemies.json5`, `src/ai/*`, `src/engine/floating_text_manager.odin`,
  `src/engine/engine.odin`, `src/game_services.odin`, `src/render/services.odin`,
  `src/render/scores.odin`, `src/render/clay_*.odin`, `src/input/state_events.odin`, `src/scene.odin`,
  plus their tests under `test/`
- **Risk:** med (D8, plus D4 deferred to Batch 8)
- **Apply note:** Gameplay tuning + features cluster. Sequence within batch: D6 (test-only, already
  fixed) → D2 (data + starter gear) → D7 (scoring, scores.odin) → D1 (flagged detection) → D8
  (floating text manager + poison_cloud, new engine service id 13) → D3 (level-up, flagged, new
  Game_State .Viewing_Level_Up). Multiple items touch `gameplay_tuning.odin`, `types.odin` (Game
  struct + Game_State enum), `events.odin`, `scene.odin`, `clay_*` — sequential edits, append new
  enum/struct fields at end. D3 and D7 both read `game.kills`; no field conflict. Two new
  feature flags ship: LIGHT_AFFECTS_DETECTION=false, LEVELUP_ENABLED=true. NOTE: this is a large
  batch — may split D8 (med risk, engine-touching) into its own sub-commit if review prefers.

### Batch 8 — `feat: first-encounter onboarding hints`
- **Items:** D4
- **Files:** `src/core/types.odin`, `src/gameplay/onboarding.odin`, `src/gameplay/actions.odin`,
  `src/gameplay/mining.odin`, `src/gameplay/items.odin`, `src/gameplay/events.odin`,
  `src/gameplay/common.odin`, `src/io/save_format.odin`, `src/io/save_write.odin`,
  `src/io/save_restore.odin`, `src/io/save_migrations.odin`, `test/gameplay/onboarding_test.odin`,
  `test/io/save_migration_test.odin`
- **Risk:** med
- **Apply note:** Isolated AFTER M1-P5 (Batch 6) because D4 ALSO mutates the save format — it appends
  `tutorial_flags` to the (now v11) Save_Data and adds a v10→v11 migration branch. If M1-P5 dropped
  legacy support, D4's migration simplifies accordingly. D4 must be the LAST save-format mutator so
  it builds on the final layout. Touches the same gameplay trigger sites (actions/mining/items/events)
  as Batch 7 — land after Batch 7 to avoid re-editing those procs.

### Batch 9 — `ci: run the real test suite; docs cleanup`
- **Items:** T1, T2, docs-drift-readme-structure
- **Files:** `.github/workflows/ci.yml`, `test/io/save_roundtrip_test.odin`, `README.md`
- **Risk:** low
- **Apply note:** Last. T1 drops the duplicate `-collection:libs=vendor/` from the 5
  `run_odin_tests.py` invocations (keeps it on direct `odin` lines) so CI stops aborting. T2 fixes the
  one wrong V4 assertion in the staged roundtrip test (`items_found` cannot survive V4 migration →
  expect 0). docs item is already-fixed; optional README corruption cleanup. CI must go green only
  after all code batches land. Disjoint files.

---

## Per-item detail

### C1 — Atomic save writes: finish .bak recovery + tests
- **Approach:** Atomic-write mechanism (rename vtable, `.tmp`/`.bak` sequencing, v10 CRC32) is already
  uncommitted in the tree and passes `just check`. Two gaps remain: the rolling `.bak` is written but
  never read on load, and there are zero tests. Add a `.bak` load fallback in `load_game_from_storage`
  and the regression tests; commit the whole set together.
- **Edits:** In `load_game_from_storage`, wrap read/header-validate/migrate in a retry over
  `{path, path+".bak"}`; on successful one-load consume, also remove `path+".bak"` and `path+".tmp"`;
  add `import "core:strings"` (grep first). Production save_write/file_system/storage_manager edits are
  ALREADY in tree — do NOT re-apply.
- **Tests:** `storage_manager_rename_delegates_to_configured_file_system`,
  `save_game_writes_to_temp_then_renames_over_target_keeping_one_backup`,
  `save_game_falls_back_to_direct_write_when_rename_is_unavailable`,
  `load_game_recovers_from_backup_when_primary_save_is_corrupt`.
- **Verify:** `just verify`; isolate-run the new procs; manual desktop smoke (flip/truncate save,
  confirm `.bak` recovery; confirm `.dat/.tmp/.bak` all gone after a normal load).
- **Open questions:** Keep or delete `.bak` after one-load consume (default: delete, per permadeath +
  no-stale-files rule)? fsync directory for power-loss durability (out of scope)? Confirm in-tree
  atomic-write changes are intended and committed with this load-side fallback.

### C2-R1-save-deser-clamp — Save deserialization count-clamping
- **Approach:** `clamp_save_counts` already clamps all file-controlled counts in every version branch;
  the OOB read is closed. Actionable: the single existing save test is FAILING (v10 CRC widened header
  8→12 bytes; test still writes V8 payload at the wrong offset). Repair it to use the legacy 8-byte
  header, then add v9 round-trip, v10+CRC round-trip (with tampered-CRC rejection), and a corrupt-count
  clamp test. Test-only.
- **Edits:** All in `test/io/save_migration_test.odin` — fix V8 buffer layout (Save_Header_Legacy);
  append v9/v10/clamp test procs; add `import "core:hash/crc32"`.
- **Tests:** `v8_save_data_migrates_legacy_status_fields_into_player_status` (repair),
  `v9_save_data_round_trips_through_load_save_data`,
  `v10_save_data_round_trips_with_valid_crc32`,
  `load_save_data_clamps_out_of_range_counts_to_capacity`.
- **Verify:** V8 test must go from FAIL→PASS; clamp test must fail if `clamp_save_counts` is removed.
  Recompute struct sizes in-test via `size_of`, do not hardcode.
- **Open questions:** R2 (post-restore sanity gate before delete) — defensive, fold into C1 or defer.
  R3 (exact-equality size gate) — do NOT relax. Full Game round-trip belongs in a package-main test
  (follow-up).

### E4 — Delete dead engine code
- **Approach:** Grep proves only THREE of five listed symbols are dead. Delete Energy_Actor block,
  scene_update (leaks 6 service blocks per call), camera_manager_abs. **Audit corrections:** input_tick
  does not exist; reset_repeats is LIVE (5 call sites) — do NOT delete either.
- **Edits:** `turn_manager.odin` lines 37-70 (Energy_Actor); `scene.odin` lines 153-168 (scene_update);
  `camera_manager.odin` lines 84-90 (camera_manager_abs). No test edits.
- **Tests:** None added; existing turn_manager/camera_manager tests are the compile-guard.
- **Verify:** `just verify`; post-edit grep must show zero matches for deleted symbols and the 5 live
  reset_repeats sites intact.
- **Open questions:** None.

### G6 — Delete dead g_data registry + data_load_all wrapper
- **Approach:** Production always uses `data_load_all_into(&content.registry)`; the `content==nil`
  fallback and thus `g_data`/`data_load_all` are dead. Delete them; replace nil-fallback with a nil
  guard; replace the alias-isolation test with a nil-guard test.
- **Edits:** `data.odin` (delete g_data global + data_load_all proc); `content_manager.odin` (nil guard);
  `core/data_defs.odin` (comment); `content_manager_test.odin` (replace test).
- **Tests:** `content_manager_load_all_returns_false_for_nil_manager`.
- **Verify:** grep finds no `g_data`/`data_load_all\b` except `data_load_all_into` and the
  saveload_dependency_test string literal; embedded-data loader path still passes.
- **Open questions:** None.

### G4 — De-duplicate FOV shadowcasting into gcore.compute_fov
- **Approach:** FOV shadowcast is duplicated in `gameplay/fov.odin` and `io/save_common.odin`. Move the
  canonical impl into new `src/core/fov.odin` (package core, keeps the io copy's nil guard), delete both
  copies, re-export `compute_fov :: gcore.compute_fov` in save_common, and make gameplay's `compute_fov`
  a thin debug-logging wrapper around `gcore.compute_fov`.
- **Edits:** create `src/core/fov.odin`; delete FOV block + local effective_light_bonus from
  `save_common.odin` + add alias; rewrite `gameplay/fov.odin` body; leave `gameplay/common.odin` alone
  (no alias — would collide with the wrapper).
- **Tests:** `gcore_compute_fov_radius_zero_only_reveals_origin_tile`,
  `gcore_compute_fov_wall_blocks_tile_behind_it`, `gcore_compute_fov_returns_safely_on_nil_game`;
  existing `test/fov_test.odin` unchanged.
- **Verify:** no duplicate-symbol errors (three distinct package scopes); grep
  `cast_light|is_opaque|OCTANT_MULTIPLIERS` returns only new core/fov.odin.
- **Open questions:** core helper naming (fov_cast_light vs cast_light) — project style. Coordinate the
  shared `save_common.odin` edit with G5 (disjoint line ranges).

### G5 — De-duplicate floor-snapshot lifecycle helpers into gcore
- **Approach:** `saved_floor_destroy` + `clear_visited_floors` are byte-identical in `save_common.odin`
  and `generation.odin`. Promote to new `src/core/floor_snapshot.odin` (package gcore, needs
  `import "base:runtime"`), delete both copies, re-qualify callers to `gcore.*`. `game_cleanup` stays
  per-package (definitions differ).
- **Edits:** create `core/floor_snapshot.odin`; delete copies from `save_common.odin` (+ drop now-unused
  `import "base:runtime"` if no other use) and `generation.odin` (KEEP its runtime import); re-qualify
  callers in generation.odin (×2), game_lifecycle.odin, save_common.odin.
- **Tests:** `saved_floor_destroy_frees_dynamic_arrays_and_zeroes_the_floor`,
  `clear_visited_floors_frees_every_heap_allocated_snapshot_and_nils_slots`.
- **Verify:** new core runtime import is used; save_common runtime import removed if unused; only
  definitions live in floor_snapshot.odin; all callers gcore-qualified.
- **Open questions:** None.

### M1-P5 — Data-model diet: drop dead Tile fields; serialize Tile_State directly
- **Approach:** `Tile.visible/explored/light_level` are the serialization carrier for the engine
  tile-state layer (live source = Tile_State_Manager). Replace the carrier: add a dedicated
  `[N]eng.Tile_State` array to Saved_Floor + Save_Data + Save_Floor, route via
  `tile_state_manager_export/import`, drop the three Tile fields, delete the bridge procs, bump v10→v11
  with a v10 migration that lifts inline Tile vis/light into the new array. Tile ~16→~8 B; Game ~32 KB
  smaller.
- **Edits:** see spec — types.odin (Tile shrink, Saved_Floor field), game_utils.odin (new
  export/import procs), save_format.odin (v11 constants, tile_states fields, frozen Save_Data_V10 +
  Tile_V10), save_write/restore/convert (copy tile_states), save_migrations.odin (v10→v11 lift +
  legacy-branch handling), gameplay/common.odin + io/save_common.odin (alias renames),
  generation.odin (snapshot export/import).
- **Tests:** `save_load_preserves_engine_tile_state_layer` (updated),
  `v10_save_migrates_tile_visibility_into_dedicated_tile_state_array`,
  `v11_save_data_layout_has_expected_byte_size_relationship_to_v10`,
  `v9_save_still_loads_after_tile_struct_shrink`.
- **Verify:** save tests pass; grep proves dead fields gone (only Tile_State usages remain); measure
  Game/Tile shrink via size assertion.
- **Open questions:** **DECISION:** drop legacy v2–v10 read support? (default per repo rule → yes,
  collapses HIGH→LOW). tile_states layout append-last vs after-tiles (recommend append-last). Single
  array vs split parallel arrays (recommend single).

### P1 — temp_allocator → per-frame arena (already-fixed)
- **Approach:** The 2-line fix (set `context.temp_allocator` to the frame arena in both engine_step
  blocks) is already in the working tree, uncommitted. Commit it + add a headless regression test that
  asserts temp_allocator equals the frame arena inside update/render callbacks.
- **Edits:** No code change needed (verify the 2 lines present); add capture fields +
  `engine_step_points_temp_allocator_at_frame_arena_during_update_and_render` test using fake backends.
- **Tests:** `engine_step_points_temp_allocator_at_frame_arena_during_update_and_render`.
- **Verify:** `git diff` shows the +2 lines; isolate-run the new test; `just verify`.
- **Open questions:** Confirm the uncommitted fix should be committed now. Compare Allocator equality
  field-wise (.procedure + .data), not whole-struct.

### P3 — Cache parsed scores.json
- **Approach:** Overlay calls `score_manager_load()` (disk read + json.unmarshal) every frame. Add a
  cache on the Score_Manager singleton with write-through invalidation (only in-process writer is
  `score_manager_save`). Load returns a deep CLONE (callers `defer score_table_destroy`); cache strings
  on `runtime.default_allocator`.
- **Edits:** scores.odin — add `cached`/`cache_loaded` fields, `score_table_clone`,
  `score_manager_cache_destroy`; rewrite `score_manager_load` (cache-or-read+cache) and
  `score_manager_save` (refresh cache after write). Add `core:strings`, `base:runtime` imports.
- **Tests:** `score_manager_load_reads_disk_once_then_serves_from_cache`,
  `score_manager_save_refreshes_cache_so_next_load_sees_new_entry`,
  `score_manager_load_returns_owned_clone_safe_to_destroy`,
  `score_manager_load_with_no_file_caches_empty_table_without_reread`.
- **Verify:** tests assert read_count==1 across loads; existing score tests still pass; manual overlay
  render unchanged.
- **Open questions:** Cached strings leak once at exit (harmless, no shutdown hook). mtime cache rejected
  (no stat in vtable) — confirm write-through acceptable.

### P2 — Minimap scratch index buffer
- **Approach:** Per-cell linear `enemy_at`/`npc_at` scans (4000 cells × E+N) replaced by two flat
  `[N]i32` scratch grids built once per minimap render, read O(1) per cell. First-match semantics
  preserved (write only when slot is -1); off-grid/dead entities skipped (no OOB write under
  -no-bounds-check). Visuals bit-identical.
- **Edits:** clay_minimap.odin — add `Minimap_Entity_Scratch` struct + `minimap_build_entity_scratch`;
  build scratch in `clay_render_minimap`; change `clay_minimap_cell_color` signature to take scratch;
  read scratch instead of scans (hoist `idx`, delete duplicate). test_imports_test.odin — add aliases.
- **Tests:** `minimap_entity_scratch_matches_linear_scans_for_every_cell`,
  `minimap_entity_scratch_skips_dead_and_off_grid_entities`,
  `minimap_entity_scratch_records_first_enemy_when_two_share_a_tile`.
- **Verify:** equivalence test is the proof; existing explore-cheat test unchanged; manual minimap toggle.
- **Open questions:** 32 KB stack local acceptable (prefer over a global). Texture-regeneration stretch
  goal out of scope.

### P4 — Dedup render_map tile-color + (optional) color cache
- **Approach:** TWO sub-fixes. (1) DEDUP: `get_tile_color` is correct but DEAD; render_map reimplements
  it inline — replace the inline block with a `get_tile_color` call (provably equivalent, all base
  colors have a=255). (2) CACHE: three render-cache Game fields are dead. **Plan adopts dedup-only +
  delete dead fields; defer the actual color cache to post-P8** per the spec's recommendation.
- **Edits:** render_map.odin — replace inline tint block with `get_tile_color(...)`. types.odin — delete
  dead `render_last_cam_x/render_last_cam_y` and (cache deferred) `render_map_dirty` + its writer at
  game_utils.odin:86. (Cache-step edits NOT applied.)
- **Tests:** `get_tile_color_matches_legacy_inline_math_for_visible_explored_and_unseen` (dedup lock).
  Cache test only if cache implemented (deferred).
- **Verify:** pixel-identical; `get_tile_color` no longer dead; manual lit-torch warmth check.
- **Open questions:** **DECISION adopted:** dedup now, defer cache to post-P8 (avoids +16 KB to an
  already-oversized Game struct for unmeasured gain).

### P6-P7 — Dijkstra BFS hoist (P6) + flow-field cache (P7)
- **Approach:** P6: expose the dead `_distance_map_*_unchecked` accessors, validate once at top of
  `compute_dijkstra_map`, switch BFS to unchecked (BFS already bounds-checks neighbors). P7: add
  `dijkstra_dirty` to Game; `process_enemy_turns` recomputes only when dirty; `trigger_enemy_rounds`
  marks dirty once per player input; `compute_dijkstra_map` clears it. 1 recompute/input instead of N.
- **Edits:** distance_map.odin (public unchecked accessors); enemy_pathfinding.odin (is_valid guard +
  unchecked accessors + clear dirty); enemy_turns.odin (gate on dirty); actions.odin (mark dirty);
  types.odin (`dijkstra_dirty: bool`). enemy_spawn.odin unchanged.
- **Tests:** `engine_distance_map_unchecked_accessors_read_and_write_without_validation`,
  `dijkstra_map_is_only_recomputed_when_marked_dirty`,
  `trigger_enemy_rounds_recomputes_flow_field_once_then_reuses_it_across_slow_player_rounds`.
- **Verify:** **CRITICAL** — every direct `process_enemy_turns` test caller (5 in
  enemy_pathfinding_test, plus enemy_turns_test) must set `dijkstra_dirty=true` first or they read an
  unreachable map and movement assertions break. Spot-check `visible_enemy_moves_downhill_toward_player`.
- **Open questions:** FOV/render validity hoist deferred (higher risk, marginal). Mark dirty on mid-loop
  terrain mutation (mining/abilities) if any exists. `dijkstra_dirty` need not persist in saves; confirm
  no size-gate break.

### R10 — Force cardinal movement (already-fixed)
- **Approach:** Diagonal-collapse guard `if dx!=0 && dy!=0 {dy=0}` already present in
  `read_cardinal_press` and `handle_input`; mining inherits it. Add the missing regression test only.
- **Edits:** create `test/input_cardinal_movement_test.odin` (package main, #+build !js), reusing
  Test_Input_Backend_State / test_input_backend from input_manager_test.odin.
- **Tests:** `read_cardinal_press_collapses_diagonal_to_horizontal_axis_only`,
  `read_cardinal_press_preserves_pure_cardinal_and_idle_input`.
- **Verify:** run the two procs; `just verify`.
- **Open questions:** is_walkable corner-cut guard unnecessary while input is single-axis; revisit if a
  future diagonal-intent path (mouse/knockback) appears.

### R11 — Route Escape during Playing to a Pause / confirm menu
- **Approach:** Replace the in-tree double-Escape `quit_armed` (which kills the process, not returns to
  title) with a real Pause scene: new `.Pause` Game_State + Game_Scene + scene mapping + registration +
  `update_pause` (Resume / Quit-to-Title) + `clay_render_pause_overlay`. update_playing intercepts
  `.Quit` before handle_player_action; Quit-to-Title sets `.Title_Screen` (process stays alive).
- **Edits:** types.odin (.Pause Game_State, `pause_choice`); scene.odin (GAME_SCENE_COUNT++,
  .Pause scene, mapping, registration, game_scene_pause_update); state_updates.odin (intercept .Quit,
  update_pause); playing_action.odin (remove quit block); state_shared.odin (remove quit_armed);
  clay_screen_ui.odin (.Pause render case); clay_overlays.odin (overlay; do NOT bool-wrap clay.UI).
- **Tests:** `escape_during_play_opens_the_pause_menu_instead_of_quitting`,
  `pause_menu_resume_returns_to_playing`,
  `pause_menu_quit_to_title_returns_to_title_without_exiting_process`,
  `pause_menu_escape_resumes_play`, `pause_menu_down_then_up_cycles_selection`.
- **Verify:** `just verify`; manual Escape→pause card, navigation, Quit-to-Title without closing window;
  `graphify update .`.
- **Open questions:** Add a true Quit-Game (process exit) row? Confirm menu-confirm action name. Score/
  save an abandoned run on Quit-to-Title (spec discards)?

### D6 — Depth-merchant currency (already-fixed)
- **Approach:** `cost_id="iron_ore"` fix already staged (was ITEM_ID_RUSTY_PICKAXE, an equip-only weapon
  invisible to inventory_count_item_type). Add the missing regression test only.
- **Edits:** events.odin already done (verify, do not re-edit); append test to town_story_test.odin.
- **Tests:** `depth_merchant_uses_inventory_currency_and_buy_consumes_it`.
- **Verify:** new test fails if cost_id reverts; `just verify`.
- **Open questions:** depth-varying currency is a separate design item.

### D2 — Early-game tuning: starter leather vest + re-band enemies
- **Approach:** Ranged-gate already done (do NOT touch enemy_abilities.odin). Add `leather_vest` to
  starter gear (into equipped_armor), add ITEM_ID_LEATHER_VEST constant, and re-band flesh_brute /
  grasping_root out of the d3-4 spawn table (delete those two weight entries; they remain from d5).
  Chooses armor route over HP bump.
- **Edits:** content_ids.odin (constant); items.odin (give_starter_gear vest block);
  enemies.json5 (delete two d3-4 entries).
- **Tests:** `give_starter_gear_equips_a_leather_vest_for_starting_defense`,
  `flesh_brute_and_grasping_root_do_not_spawn_before_depth_five`.
- **Verify:** grep enemies.json5 (both ids only depth_min>=5); effective_defense==1 at start; confirm
  enemy_abilities.odin untouched.
- **Open questions:** Add +HP too if still punishing (defer). Starter armor makes early leather_vest
  drops redundant (likely fine).

### D8 — Combat feedback: floating damage numbers + poison_cloud ability
- **Approach:** (a) New engine `Floating_Text_Manager` (zero raylib, 32-slot pool, world-tile coords),
  service id 13, render in renderer pkg, spawn at the two combat resolution points. (b) Add the missing
  active `poison_cloud` AI branch (stamp Gas_Vent adjacent when player in range) and fix its data
  cooldown/range (was 0/0). Thread `engine` into resolve_attack_enemy_on_player for player-damage numbers.
- **Edits:** new floating_text_manager.odin; engine.odin (field + make + getter); service_ids.odin +
  game_services.odin + render/services.odin (id 13 + getters); new render/floating_text.odin;
  render.odin (update + render); combat.odin (spawn at both sites + signature change); enemy_turns.odin
  (thread engine); enemy_abilities.odin (poison_cloud branch); enemies.json5 (cooldown 4 range 4);
  playing_action.odin (pass engine); ai/common.odin (aliases).
- **Tests:** `floating_text_manager_spawns_and_expires_a_damage_number`,
  `floating_text_manager_caps_at_pool_size`,
  `poison_cloud_ability_stamps_a_gas_vent_tile_adjacent_to_the_enemy_when_player_is_in_range`,
  `poison_cloud_ability_does_nothing_when_player_is_out_of_range`.
- **Verify:** `just verify`; manual: rising numbers on hits, Gas_Vent + message from poison_cloud enemy;
  engine stays raylib-free.
- **Open questions:** Thread engine into process_enemy_turns chokepoint (advance_turn) or accept nil for
  v1. Font legibility at 12px in scissor. Add numbers for ability damage too (trivial extension).

### D1 — Player light → enemy detection (flagged)
- **Approach:** Behind `LIGHT_AFFECTS_DETECTION` (default false). Add `player_effective_light_radius`
  dedup helper + DETECTION_HEARING_RADIUS const. When flag on, sight detection only reaches full
  detection_radius inside the player's emitted light; outside, clamp to hearing radius. Flag off =
  byte-identical to today (compile-time short-circuit).
- **Edits:** gameplay_tuning.odin (flag + const); game_utils.odin (helper); ai/common.odin (3 aliases);
  enemy_turns.odin (`when LIGHT_AFFECTS_DETECTION` branch in enemy_update_awareness).
- **Tests:** `enemy_outside_light_but_in_detection_radius_becomes_aware_when_flag_off`, plus three
  `when`-guarded flag-on tests (falls_back_to_hearing, inside_light_becomes_aware,
  within_hearing_radius_in_darkness).
- **Verify:** flag-off path identical (existing tests pass); flag-on tests `when`-guarded so flag matrix
  stays green; flip flag locally to exercise. ai aliases must land or undefined-identifier errors.
- **Open questions:** Manhattan vs Euclidean light-reach metric. Tile light_level vs player radius
  (radius is robust). DETECTION_HEARING_RADIUS=3 is a guess.

### D3 — Milestone kill-driven level-up menu
- **Approach:** Behind `LEVELUP_ENABLED` (default true). Reuse Shrine_Buff enum as a level-up choice
  with NO HP cost. Derive `player_level` from persisted `kills` (no save bump). On crossing a kill
  threshold, queue `pending_level_ups` and open new `.Viewing_Level_Up` state; apply via
  `apply_levelup_buff`.
- **Edits:** gameplay_tuning.odin (flag + thresholds + buff values); types.odin (player_level,
  pending_level_ups, level_choice; .Viewing_Level_Up Game_State APPENDED last); events.odin
  (levelup_kills_for_level, levelup_level_for_kills, apply_levelup_buff, check_level_up);
  actions.odin (trigger in handle_player_moved); state_events.odin (update_viewing_level_up,
  non-dismissable); scene.odin (dispatch + mapping); clay_events.odin + clay_screen_ui.odin (overlay);
  save_restore.odin (derive player_level).
- **Tests:** 7 procs — threshold math (×2), check_level_up queueing (×2), apply buff no-HP-cost,
  re-open while pending, max_hp raises cap+current.
- **Verify:** `just verify`; manual 8 kills → menu, no HP cost; reload resumes at correct level WITHOUT
  spurious menu. Flag-matrix: if toggled via -define, use runtime `if` not `when`.
- **Open questions:** **save-restore package boundary** — recommend moving the two pure derivation procs
  into core (gcore) so both gameplay and gameio can use them. Threshold/magnitude tuning. Full heal vs
  +N on Max_HP. Mandatory vs cancelable menu.

### D7 — Numeric run score + victory multiplier
- **Approach:** Add `score: int` + `victory: bool` to Score_Entry (json tolerates missing keys →
  backward-compatible, no migration). Add pure `compute_run_score(...)` with VICTORY_MULTIPLIER +
  VICTORY_BONUS. save_run_score sets victory + score; insert_score ranks by score first. Surface
  "Final Score" in the victory overlay.
- **Edits:** scores.odin (fields, constants, compute_run_score, insert_score sort);
  restart.odin (populate fields); clay_overlays.odin (Final Score KV row).
- **Tests:** `compute_run_score_awards_victory_multiplier_and_bonus`,
  `insert_score_ranks_victory_run_above_deeper_death`,
  `score_table_loads_legacy_json_without_score_field`,
  `save_run_score_records_victory_flag_on_victory_state`.
- **Verify:** `just verify`; manual victory shows non-zero Final Score; old scores.json loads.
- **Open questions:** Scoring weights are placeholders. Meta-unlock/profile file is a stretch (separate
  follow-up; `victory` bool is the data foundation). Display new-#1 indicator?

### D4 — First-encounter onboarding hints (bit_set on Game)
- **Approach:** Add `Tutorial_Flags :: bit_set[Tutorial_Hint; u8]` to Game; one-time
  `tutorial_hint_once` helper; five guarded trigger sites (first enemy seen, ore, torch, status,
  shrine). Persist via save v11 (bump from M1-P5's v11 by appending `tutorial_flags` as trailing field
  + v10→v11 migration). Flags survive descent; cleared on restart.
- **Edits:** types.odin (enum + bit_set + Game field); new onboarding.odin; actions.odin (enemy+status
  triggers post-FOV); mining.odin (ore); items.odin (torch); events.odin (shrine);
  game_lifecycle.odin (clear on reinit); common.odin (aliases); save_format/write/restore/migrations
  (tutorial_flags + migration).
- **Tests:** `tutorial_hint_once_fires_a_message_only_on_the_first_call`,
  `tutorial_flags_persist_across_a_floor_descent`,
  `v10_save_data_migrates_with_zeroed_tutorial_flags`,
  `tutorial_flags_survive_a_v11_save_and_restore_roundtrip`.
- **Verify:** `just verify`; manual each hint fires once and does not repeat after descent; save/reload
  suppresses seen hints; old save migrates. **Apply AFTER M1-P5** — coordinate the v11 layout so
  tutorial_flags is the final field and size relationships hold.
- **Open questions:** Reset flags on restart (spec: yes per-run, persist per-save). Toast vs message-log
  (spec: log only). Hint copy is placeholder.

### T1 — Fix CI: drop duplicate -collection:libs flag
- **Approach:** Staged ci.yml already runs run_odin_tests.py + flag matrix + build-web, but passes a
  redundant `-collection:libs=vendor/` to the runner (which already injects it) → Odin aborts
  ("collection 'libs' already exists"). Purely subtractive: remove the flag from the 5 runner
  invocations; KEEP it on the 3 direct `odin` lines (check/Type check/Build).
- **Edits:** ci.yml lines 50, 54-58 (remove flag); lines 59, 62, 65 unchanged.
- **Tests:** None (CI config).
- **Verify:** simulate each CI line locally post-edit → "All tests were successful" not the collection
  error; negative check (re-adding flag reproduces failure); green CI run.
- **Open questions:** Optionally harden run_odin_tests.py to skip self-injection if a collection is
  already supplied (defer).

### docs-drift-readme-structure — README/CLAUDE.md docs drift (already-fixed)
- **Approach:** Cited finding already satisfied (README project tree current; CLAUDE.md says ~253 KB).
  Separate real defect found: corrupted/duplicated "Backlog" block in README (~lines 188-196,
  "ta packs" orphan + duplicated heading/bullets). Optional cleanup.
- **Edits:** README.md lines 188-196 — remove the garbled duplicated block, leave one Backlog heading +
  the real bullets. (Optional; finding needs no edit.)
- **Tests:** None (docs).
- **Verify:** grep confirms 253 KB + sub-package tree; if cleanup applied, single Backlog heading, no
  "ta packs".
- **Open questions:** Apply the optional README corruption cleanup now or track separately?

### T2 — Save-migration regression tests: fix one wrong V4 assertion
- **Approach:** The staged `save_roundtrip_test.odin` already covers every migrate branch + round-trip +
  corruption/truncation/oversize/clamp. One genuine test bug: `v4_save_data_migrates_into_current_layout`
  asserts `items_found==8`, but Save_Data_V4 diverges from current at skip_next_turn(bool) vs
  web_stuck_turns(int) — items_found cannot survive a V4 prefix mem.copy. Fix the assertion to expect 0
  + document why. Keep depth/kills (safe prefix).
- **Edits:** save_roundtrip_test.odin — delete `old.items_found = 8`; change assertion to
  `expect_value(items_found, 0)` with explanatory comments.
- **Tests:** `v4_save_data_migrates_into_current_layout` (repair); 9 others already present, no change.
- **Verify:** src/io package goes 1-fail→all-pass; `just verify`.
- **Open questions:** CI wiring is T1, not T2. V4 items_found is unrecoverable by design (dropping is
  acceptable for a pre-release format) — flag if a hand-written V4 migrator is wanted.
